import cv2
import numpy as np
from redbird import LandmarkLib
from redbird import Camera

#INITIALIZERS

#CAMERAS
cam0 = Camera(1, (1280, 720), 60, (130, 90), (0, 45))
cam1 = Camera(1, (1280, 720), 60, (130, 90), (180, 45))

#INSTANCES
redgoal = Landmark()
greengoal = Landmark()
landmarkList[redgoal, greengoal]

while True:

    #Get Frame List
    
    #Search for Landmarks

    #Publish to Node

    #Show Frame
    
